# Required — sign up at console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Optional — only needed for "Original Couture" (Mode B) image generation.
# Sign up at huggingface.co/settings/tokens (free)
# Without this, the app still works in "Archive" mode (Mode A).
HUGGINGFACE_API_KEY=hf_...
